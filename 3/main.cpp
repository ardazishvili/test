#include "Matrix.h"
#include "Vector.h"
#include <iostream>

int main(int argc, char** argv)
{
  /* auto m = mat3({ 1, 2, 4 }); */
  auto v = vec3(1, 2, 3);
  /* auto wrong = vec3(1, 2, 3, 4); */
  std::cout << "v= " << v << std::endl;

  std::cout << "ki" << std::endl;

  return 0;
}
